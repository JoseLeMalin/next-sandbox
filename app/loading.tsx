import { Skeleton } from "@/components/ui/skeleton";

export default function RootLoading() {
  // You can add any UI inside Loading, including a Skeleton.
  return <Skeleton className="h-[80%] w-[50%] rounded-full" />;
}
